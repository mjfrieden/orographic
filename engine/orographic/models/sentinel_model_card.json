{
  "artifact": "sentinel_model",
  "version": 1,
  "model_card_schema_version": 1,
  "trained_at": "2026-06-27",
  "activation_policy": {
    "default": "shadow",
    "active_env": "OROGRAPHIC_SENTINEL_MODE=active",
    "reason": "Sentinel remains an event overlay until live disagreement evidence supports promotion."
  },
  "structured_output": {
    "mode": "structured_event_artifact",
    "fields": [
      "event_type",
      "event_polarity",
      "directional_relevance",
      "novelty",
      "source_reliability",
      "time_horizon",
      "confidence",
      "direction_1d",
      "direction_3d",
      "direction_5d",
      "magnitude_bucket",
      "decay_half_life",
      "spot_vs_iv_effect",
      "call_relevance",
      "put_relevance",
      "no_trade_relevance",
      "structured_event_score"
    ],
    "decision_contract": "Remote headline extraction is optional. The local Sentinel artifact backfills, normalizes, and scores structured event fields from event_context plus any extracted event metadata."
  },
  "training_policy": {
    "family": "deterministic_structured_event_model",
    "inputs": [
      "Cloudflare Sentinel extraction payload",
      "event_context feature store",
      "event profile priors"
    ],
    "notes": [
      "This first-stage Sentinel artifact is a local structured policy model, not a supervised classifier.",
      "Future promotion should replace or calibrate these weights with labeled live-event outcomes."
    ]
  },
  "artifacts": {
    "model_path": "engine/orographic/models/sentinel_model.json",
    "model_sha256": "8cbeb8e6dc964a1181561930e27fc7cc2255e1ff13916a8f658a8103baa23002",
    "model_card_path": "engine/orographic/models/sentinel_model_card.json"
  }
}
